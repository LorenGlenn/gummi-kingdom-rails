<h1>Gummi-Kingdom!</h1>

<% if @products.any? %>
  <% @products.each do |p| %>
    <ul>
      <li><%= link_to p.name, product_path(p) %></li>
    </ul>
    <% end %>
<% else %>
  <p>
    There are no products yet! Click <%= link_to "here", new_product_path %> to add one!
  </p>
  <% end %>
